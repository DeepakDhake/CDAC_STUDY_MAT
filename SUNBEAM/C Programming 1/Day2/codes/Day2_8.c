#include<stdio.h> 

int main( )
{
    //Arithmatic opr( + , - , * , / , % ) ( binary opr)
    //unary ( + , - )( 1 operand  )

    int num = +1; 
    int num1 = -1;  
    // c = a + b;   

    return 0; 
}